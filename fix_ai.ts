import "dotenv/config"; import { getDb } from "./server/db.js"; import { sql } from "drizzle-orm"; async function run() { const db = await getDb(); try { await db.execute(sql`ALTER TABLE contas_convenio_itens MODIFY id INT AUTO_INCREMENT`); console.log("contas_convenio_itens id is now auto_increment"); } catch(e) { console.error(e.message); } try { await db.execute(sql`ALTER TABLE contas_convenio_resumo MODIFY id INT AUTO_INCREMENT`); console.log("contas_convenio_resumo id is now auto_increment"); } catch(e) { console.error(e.message); } process.exit(0); } run();
