import 'dotenv/config'; import { getDb } from '../server/db'; import { convenioEstabelecimentoPrestador } from '../drizzle/schema'; import { eq } from 'drizzle-orm'; (async () => { const db = await getDb(); const res = await db.select().from(convenioEstabelecimentoPrestador).where(eq(convenioEstabelecimentoPrestador.codigoPrestador, '1101060')); console.log(res); process.exit(0); })();
