import 'dotenv/config'; import { processarArquivoRpa } from '../server/rpa/importador'; import fs from 'fs'; import path from 'path'; const dir = 'uploads/demonstrativos/unimed'; const files = fs.readdirSync(dir).filter(f => f.endsWith('.xlsx')); (async () => { for (const f of files) { console.log('Processing', f); await processarArquivoRpa(path.resolve(dir, f), f, 1).catch(e => console.error(e)); } console.log('ALL DONE'); })();
