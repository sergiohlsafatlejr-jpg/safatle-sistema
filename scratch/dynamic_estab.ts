import XLSX from 'xlsx'; export function extractEstabelecimentoFromBuffer(buffer: Buffer): string | null { const wb = XLSX.read(buffer, {type: 'buffer'}); const sheet = wb.Sheets[wb.SheetNames[0]]; const rows = XLSX.utils.sheet_to_json(sheet, {header: 1}) as string[][]; for (let i=0; i<Math.min(20, rows.length); i++) { if (rows[i] && rows[i].length > 4) { const val = String(rows[i][4]); if (val && val.match(/^[0-9]+$/) && val !== 'Código Prestador Pagamento') return val; } } return null; } console.log(extractEstabelecimentoFromBuffer(require('fs').readFileSync('uploads/demonstrativos/unimed/demonstrativo-0289384.xlsx')));
