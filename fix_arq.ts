import "dotenv/config"; import { getDb } from "./server/db.js"; import { sql } from "drizzle-orm"; async function run() { const db = await getDb(); try { await db.execute(sql`ALTER TABLE arquivos ADD PRIMARY KEY (id)`); } catch(e) { console.log(e.message); } try { await db.execute(sql`ALTER TABLE arquivos MODIFY id INT AUTO_INCREMENT`); console.log("OK"); } catch(e) { console.error(e.message); } process.exit(0); } run();
