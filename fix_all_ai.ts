import "dotenv/config"; import { getDb } from "./server/db.js"; import { sql } from "drizzle-orm"; async function run() { const db = await getDb(); const [rows] = await db.execute(sql`SELECT TABLE_NAME, COLUMN_NAME FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND COLUMN_NAME = "id" AND DATA_TYPE = "int" AND EXTRA != "auto_increment"`); for(const row of rows) { try { await db.execute(sql.raw(`ALTER TABLE ${row.TABLE_NAME} ADD PRIMARY KEY (id)`)); } catch(e){} try { await db.execute(sql.raw(`ALTER TABLE ${row.TABLE_NAME} MODIFY id INT AUTO_INCREMENT`)); console.log("Fixed", row.TABLE_NAME); } catch(e) { console.log("Failed", row.TABLE_NAME, e.message); } } process.exit(0); } run();
